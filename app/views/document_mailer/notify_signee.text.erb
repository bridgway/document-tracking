Hello <%= @document.signee.name.split.first %>,

<%= format_paragraph @document.message %>

Here is a link to view the document:

<%= File.join $APP_BASE_URL, public_discussion_url(@document.user, @document, :token => true) %>

<%= @document.user.name %>